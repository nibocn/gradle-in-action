version = '0.1-SNAPSHOT'

task printVersion {
    doLast {
        logger.quiet "[quiet] Version: $version"
        logger.info "[info] Version: $version"
        logger.warn "[warn] Version: $version"
        logger.error "[error] Version: $version"
    }
}

